#include<stdio.h>

int main()
{
    float pricePen = 10, pricePencil = 5, priceSharpner = 2, totalPrice;
    totalPrice = (pricePen * 2) + (pricePencil  * 3) + (priceSharpner * 1);
    printf("Total price = %f", totalPrice);
}