#include <iostream>

using namespace std;

class triangle {

    float area(float base, float height, float hypotenuse) {
        float area = base * height / 2;
        return area;
    }

    float area(float base, float side) {
        float height;
    }
};